你现在的任务是：快速理解一个新项目的整体架构和业务逻辑，并将结果沉淀为“项目上下文文档”，用于后续复用。

━━━━━━━━━━━━━━━━━━━━
【最终目标】
生成一个高质量的项目上下文文件，并写入：

docs/ai-context/PROJECT_CONTEXT.md

━━━━━━━━━━━━━━━━━━━━
【分析目标】
- 构建项目整体架构视图（高层）
- 理解核心模块划分
- 梳理主要业务流程（数据流 / 调用链）
- 输出可长期复用的结构化文档

━━━━━━━━━━━━━━━━━━━━
【范围限制（非常重要）】
- 允许扫描项目目录结构
- 只读取关键文件（入口文件 / 核心模块）
- 禁止递归扫描全部文件
- 跳过以下目录：
  - .git
  - logs
  - .venv
  - node_modules
  - build
  - dist

━━━━━━━━━━━━━━━━━━━━
【推理限制（防卡死）】
- 不要进行深度推理
- 不要构建完整依赖图
- 不要逐文件解释
- 不要尝试理解所有代码细节

━━━━━━━━━━━━━━━━━━━━
【复杂度限制】
- 控制在快速分析级别（总时间控制3分钟以内）
- 如果信息不足，不要扩大扫描范围
- 基于已有信息直接总结

━━━━━━━━━━━━━━━━━━━━
【输出内容结构（必须严格遵守）】

请生成一个完整的 Markdown 文档，结构如下：

# PROJECT CONTEXT

## 1. 项目类型
（如：Web服务 / 调度系统 / AI平台 等）

## 2. 技术栈（推测）
- 后端：
- 前端：
- 存储：
- 中间件：

## 3. 项目结构（高层）
（目录树，最多3层）

## 4. 核心模块划分
（每个模块一句话说明）

## 5. 主要业务流程
（3~6步描述主流程）

## 6. 系统角色关系
（如：API / Scheduler / Worker）

## 7. 关键入口文件
（列出主入口）

## 8. 总体理解（总结）
（不超过5句话）

━━━━━━━━━━━━━━━━━━━━
【文件写入要求】

- 将最终结果写入：
  docs/ai-context/PROJECT_CONTEXT.md

- 如果目录不存在，请先创建：
  docs/ai-context/

- 如果文件已存在：
  - 直接覆盖（不要追加）
  - 保持结构一致

━━━━━━━━━━━━━━━━━━━━
【停止条件】

- 一旦完成“高层架构认知”并生成文档，立即停止
- 不要继续深入分析代码细节
